---
description: 
---

# Bench Build Workflow

When building the frontend or checking for build errors, NEVER use `yarn build` directly. ALWAYS use the following command instead:
`bench build --app erpnextkta --production --force`
